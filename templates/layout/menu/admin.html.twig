{% if is_granted("ROLE_ADMIN") %}
    <li>
        <a href="#"><i class="icon-users4"></i> <span>{{ "User Management"|trans }}</span></a>
        <ul>
            <li {% if app.request.getPathInfo|split('/admins')|length > 1 %} class="active"{% endif %}>
                <a href="{{ path('admins_list') }}">{{ "Administrators"|trans }}</a>
            </li>
        </ul>
    </li>
{% endif %}